import { SchoolDaysPipe } from './school-days.pipe';

describe('SchoolDaysPipe', () => {
  it('create an instance', () => {
    const pipe = new SchoolDaysPipe();
    expect(pipe).toBeTruthy();
  });
});
